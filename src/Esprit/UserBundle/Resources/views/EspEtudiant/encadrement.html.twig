{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Liste des étudiants </h1>

     <table class="table table-bordered table-striped table-condensed table-hover" style="width: 80%; margin-left: 10%;">
             <thead>
            <tr>
               
                <th>Nomet</th>
                <th>Pnomet</th>
                <th>Sujet</th>
                <th>Date de début</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.nomEt }}</td>
                <td>{{ entity.pnomEt }}</td>
                <td>{{ entity.fiche.sujet }}</td>
                <td>{{ entity.fiche.datedebut|date('Y-m-d H:i:s') }}</td>
                
                <td>
              
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
      
    </ul>
    {% endblock %}
