{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FichePfe list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sujet</th>
                <th>Datedebut</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('fichepfe_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sujet }}</td>
                <td>{% if entity.datedebut %}{{ entity.datedebut|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('fichepfe_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('fichepfe_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('fichepfe_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
